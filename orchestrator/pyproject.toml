[project]
name = "orchestrator"
version = "0.1.0"
description = "Voice agent orchestrator service"
requires-python = ">=3.10"
dependencies = [
    "websockets",
    "sounddevice",
    "numpy",
    "torch==2.8.*",
    "matplotlib",
    "torchaudio",
]

