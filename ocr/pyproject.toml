[project]
name = "ocr"
version = "0.1.0"
description = "OCR monitoring tool with region selection and periodic text extraction"
requires-python = ">=3.10"
dependencies = [
    "pyautogui",
    "paddleocr",
    "paddlepaddle",
    "pillow",
    "numpy",
    "rapidfuzz",
]

